import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  sideBtn: {
    padding: 10,
    borderRadius: 9,
    marginLeft: 10,
    borderWidth: 2,
  },
  filterBtn: {
    borderWidth: 2,
    padding: 10,
    borderRadius: 9,
    flex: 1,
    alignItems: 'center',
  },
});
